  
  
  
optimizations --- 

1. keep each env in a single block we can use fast shared mem, and texture memoery for constants

2. use   -maxrregcount=59 -- too squeeze 3 blocks in a single sm_86
3. decreased num springs a bit to reduce num threads -- to fit 3 blk in sm_86
4. after 3 block in sm keep number total blocks ie pop size multiple of (num_sm * occupancy) = (20 * 3)

5. limit collision checks and handle boundary to per 8 iterations
6. use floats everywhere to increase simd Throughput -- increase vars per cache target_link_libraries

7. try to push as much as possible in single kernel since we already have everything loaded theoretical
      minimize cpu gpu copying
  
8. made bounding circle for sperm and ball -- if intersect then only do collision checks
      downside no collision checks if sperm tails hits itself
  
  
PROFILING

ncu -c 1 ./binary
compute-sanitizer --tool memcheck ./your_binary  
compute-sanitizer --tool racecheck ./your_binary 