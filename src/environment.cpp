#include "headers/environment.hpp"
#include "headers/creature.hpp"

Environment::Environment(){
    // CREATE THE BALL BEFROE THE CREATURE
    // if you do afterwords and the vector moves to a bigger size
    // all the springs which hold references will be invalidated

    // m_creature = create_creature_muscle_sperm(m_num_particles, m_particles, int &num_springs, std::vector<Spring> &springs, int &num_muscles, std::vector<Muscle> &muscles, float dt)
}

